server:
  port: 7010

spring:
  application:
    name: microservice-hystrix-turbine

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

turbine:
  app-config: microservice-movie
  cluster-name-expression: "'default'"